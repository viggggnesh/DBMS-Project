# Inventory Management Project

Made as part of a course project for the unit Database Management.

The application has been made in Python and uses packages such as `tkinter` and `sqlite3`. Using the SQLite component outside Python allows for the storage of data in a tabular format.
